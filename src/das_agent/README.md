# DAS AGENT 

## How to run

```
make run-das-agent OPTIONS="--node_id=localhost:7000 --server_id=localhost:7001 --mongo_hostname=localhost --mongo_port=27017 --mongo_username=root --mongo_password=root --redis_hostname=localhost --redis_port=6379"
```